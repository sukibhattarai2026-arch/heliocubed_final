name: Build HelioCubed GPU Docker Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
      - FullSphere

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/sukibhattarai2026-arch/heliocubed_docker

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push GPU Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.gpu
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/sukibhattarai2026-arch/heliocubed_docker:gpu-test
            ghcr.io/sukibhattarai2026-arch/heliocubed_docker:gpu-${{ github.sha }}

